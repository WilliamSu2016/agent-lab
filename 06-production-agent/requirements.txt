python-dotenv>=1.0,<2.0
openai-agents==0.22.0
langgraph>=0.2,<0.3
langgraph-checkpoint>=2.0.10,<3.0
langgraph-checkpoint-sqlite>=2.0,<3.0
typing_extensions>=4.7
pytest>=8.0,<9.0

# Deployment experiment (docs/08-DEPLOYMENT.md): the production API layer
# that wraps the durable graph (src/durable/) behind POST/GET /runs.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
# Only needed by fastapi.testclient.TestClient in tests/test_api.py.
httpx>=0.27,<1.0
